import {
  getPage,
  getPaginationLabel,
  getPaginationNextUrl,
  getPaginationPreviousUrl,
  getPathname,
  getTotal,
} from '../selectors'

describe('selectors', () => {
  describe('getPaginationLabel', () => {
    // INSERT TESTS HERE
    it('start', () => {
      expect(true).toEqual(true)
    })
  })
})
